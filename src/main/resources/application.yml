spring:
  data:
    mongodb:
      database: NyReader
  jackson:
    serialization:
      INDENT_OUTPUT: true

logging:
  level:
    org.springframework:
      security: DEBUG

jwt:
  token:
    header: Authorization
    secret: mySecret
    expiration: 604800

server:
  context-path:
